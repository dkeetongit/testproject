{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "concurrently \"npm run nuxt-dev\" \"npm run tailwind-watch\"",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "nuxt-dev": "nuxt dev",
    "tailwind-build": "npx tailwindcss build -i ./assets/styles/main.css -o ./assets/styles/styles.css",
    "tailwind-watch": "npm run tailwind-build -- --watch"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.16",
    "concurrently": "^8.2.2",
    "nuxt": "^3.9.0",
    "tailwindcss": "^3.4.0",
    "vue": "^3.3.13",
    "vue-router": "^4.2.5"
  }
}
